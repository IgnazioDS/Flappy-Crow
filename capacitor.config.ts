import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.ignaziods.flappycrow',
  appName: 'Flappy Crow',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
